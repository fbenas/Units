package utils;

/*
 * Enum to hold the type of object that fills a
 * grid space.
 * Additonally "EMPTY" represents an empty grid square
 */

public enum GridType
{
	EMPTY, HOME, RESOURCE, UNIT, GROUND
}
